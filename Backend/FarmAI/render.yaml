services:
  - type: web
    name: FarmSmartAI
    env: python
    buildCommand: "pip install -r Backend/FarmAI/requirements.txt"
    startCommand: "web: gunicorn FarmAi:app"
    pythonVersion: "3.10"
    envVars:
      - key: FARMAI_API_KEY
        sync: false
